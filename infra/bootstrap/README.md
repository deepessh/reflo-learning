# Bootstrap root

Reserved for the one-time remote-state, TableStore locking, KMS metadata, and GitHub OIDC control-plane bootstrap authorized by D-GH-5. Resource implementation requires a separately triaged issue.
