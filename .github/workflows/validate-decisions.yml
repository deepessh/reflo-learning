name: Validate decision register

on:
  pull_request:
    paths:
      - AGENTS.md
      - DECISIONS.md
      - prds/reflo-prd.md
      - scripts/validate_decisions.py
      - scripts/work-item.sh
      - scripts/test_work_item.py
      - .github/workflows/validate-decisions.yml
  push:
    branches:
      - main
    paths:
      - AGENTS.md
      - DECISIONS.md
      - prds/reflo-prd.md
      - scripts/validate_decisions.py
      - scripts/work-item.sh
      - scripts/test_work_item.py
      - .github/workflows/validate-decisions.yml

permissions:
  contents: read
  issues: read
  pull-requests: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Test governance tooling
        run: python3 -m unittest scripts/test_validate_decisions.py scripts/test_work_item.py
      - name: Check work-item helper syntax
        run: bash -n scripts/work-item.sh
      - name: Validate structure and GitHub references
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python3 scripts/validate_decisions.py --check-links
